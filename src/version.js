// The version of the app, shown in Setup. This file is loaded with the rest of
// the code, so what Setup shows is the version actually running on the device.
//
// Bump it in every change that ships, and keep package.json's "version" the
// same (a test checks).

export const VERSION = '0.7.1';
export const RELEASED = '2026-09-26';
